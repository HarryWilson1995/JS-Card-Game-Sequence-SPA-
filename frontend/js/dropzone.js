class Dropzone {
  discard(player) {
    this.addEventListener('dragover', () => {
      if (player.canDiscard === true) {
      }
    });
  }
  appendSomething() {
    dropZone.appendChild();
  }
}
