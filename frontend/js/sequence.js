class Sequence {
  static consecutiveNumbers(set) {
    set.forEach((c) => {
      const item = document.getElementById(c.id);
      console.log(item);
    });
    return true;
  }
  static sameNumbers(set) {
    set.forEach((c) => {
      const item = document.getElementById(c.id);
      console.log(item);
    });
    return true;
  }
}
